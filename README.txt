Pour lancer le site web sans problème de serveur, lancer le fichier "./usbwebserver\START.bat"

Fichiers du site web : "./usbwebserver\root"

PS : le dossier phpmailer situer dans root (qui sert a gère l'envoie de mail) ne sert pas dans le site final. 